<h2> <%= @doctor.name %> / <%= @doctor.department %> </h2>

<ul>
<% @doctor.appointments.each do |app| %>
  <li> <%= link_to app.patient.name, patient_path(app.patient) %> @ 
  <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </li>
<% end %>
</ul>