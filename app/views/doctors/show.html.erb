<h1><%= @doctor.name %></h1>
<p><i>Currently working in:</i></p>
<p><strong><%= @doctor.department %></strong></p><br>

<h2>Upcoming Appointments</h2>
<ol>
<% @doctor.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> with patient: <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
<% end %>
</ol>